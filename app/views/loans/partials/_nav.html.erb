
<nav>
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">

      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
        <div class="hidden sm:block sm:ml-6">
          <div class="flex space-x-4">
              <%= link_to 'standard', standard_path, class: current_class(standard_path) %>
              <%= link_to 'in_fine', in_fine_path, class: current_class(in_fine_path) %>
              <%= link_to 'bullet', bullet_path, class: current_class(bullet_path) %>
              <%= link_to 'linear', linear_path, class: current_class(linear_path) %>
          </div>
        </div>
      </div>
    </div>
  </div>
</nav>
